import { toast } from 'react-toastify';

const config = {
    toast: {
        position: toast.POSITION.BOTTOM_RIGHT,
        className: 'toast',
        autoClose: 8000
    }
}

export default config;